apiVersion: v1
kind: Service
metadata:
  name: allocator
  namespace: default
  labels:
    app: allocator
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: allocator
---
# Optional: Uncomment to expose via LoadBalancer
# apiVersion: v1
# kind: Service
# metadata:
#   name: allocator-external
#   namespace: default
#   labels:
#     app: allocator
# spec:
#   type: LoadBalancer
#   ports:
#     - port: 8080
#       targetPort: http
#       protocol: TCP
#       name: http
#   selector:
#     app: allocator
